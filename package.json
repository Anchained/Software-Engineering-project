{
	"name": "taskmanager",
	"version": "1.0.0",
	"main": "server.js",
	"license": "MIT",
	"dependencies": {
		"concurrently": "^9.1.2"
	},
	"scripts": {
		"client": "yarn --cwd client dev",
		"server": " nodemon  server/server.js",
		"dev": "concurrently \"yarn server\" \"yarn client\""
	},
	"repository": "https://github.com/GunalBondre/task-manager.git",
	"devDependencies": {
		"nodemon": "^3.1.10"
	}
}
